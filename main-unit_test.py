# Generated by Qodo Gen
from func import _write
import unittest

class Test_Write(unittest.TestCase):

    # Count characters in a file with multiple lines
    def test_count_characters_multiple_lines(self):
        with open('assets/original_file.txt', 'w') as f:
            f.write("Hello\nWorld\n")
        result = _write()
        self.assertEqual(result, 10)

    # Handle non-existent file gracefully
    def test_handle_non_existent_file(self):
        import os
        if os.path.exists('assets/original_file.txt'):
            os.remove('assets/original_file.txt')
        with self.assertRaises(FileNotFoundError):
            _write()

    # Manage file with special characters or symbols
    def test_manage_special_characters(self):
        with open('assets/original_file.txt', 'w') as f:
            f.write("!@#$%^&*()\n1234567890\n")
        result = _write()
        self.assertEqual(result, 20)